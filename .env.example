# Frontend (Vite) — optional backend proxy target
VITE_API_URL=http://localhost:8000

# CrewAI multi-agent pipeline (backend — not committed)
CREWAI_API_KEY=
CREWAI_BASE_URL=

# Google Gemini — research + drafting LLM (backend — not committed)
GEMINI_API_KEY=

# Optional: social platform OAuth (backend — not committed)
# INSTAGRAM_CLIENT_ID=
# INSTAGRAM_CLIENT_SECRET=
# LINKEDIN_CLIENT_ID=
# LINKEDIN_CLIENT_SECRET=
